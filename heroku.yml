build:
  docker:
    web: Dockerfile
    image: app-image
run:
  web: gunicorn church_project.wsgi:application --bind 0.0.0.0:$PORT
  image: app-image
release:
  image: app-image
  command:
    - python manage.py collectstatic --noinput
    - python manage.py migrate