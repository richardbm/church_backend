build:
  docker:
    web: Dockerfile
run:
  app: gunicorn church_project.wsgi:application --bind 0.0.0.0:$PORT
release:
  image: app-image
  command:
    - python manage.py collectstatic --noinput
    - python manage.py migrate